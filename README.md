# FastAPI Microservice

A simple FastAPI app that can be deployed on DigitalOcean App Platform and developed locally with Docker.

## Run Locally

```bash
  docker-compose up --build
```

Visit: [http://localhost:8080/run-task](http://localhost:8080/run-task)
